# Frontend Nixpacks Configuration
# Using Node.js 22.x (Nixpacks will provide latest available)

[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = [
  "node --version",
  "npm ci"
]

[phases.build]
cmds = [
  "npm run build"
]

[start]
cmd = "npx serve -s dist -l 80"

