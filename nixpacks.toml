# Force Dokploy/Nixpacks to build and run the backend app
# Using Node.js 22.x (Nixpacks will use latest available 22.x)

[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = [
  "node --version",
  "cd backend && npm ci"
]

[phases.build]
cmds = [
  "cd backend && npm run build"
]

[start]
cmd = "cd backend && node dist/index.js"
